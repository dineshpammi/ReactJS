import React, { useState } from 'react';
import './Dashboard.css';
import { postRequest } from '../../services/axios';
import { urls } from '../../services/urls';

const Dashboard = () => {
    return (
        <div> dashboard wrks</div>
    )
}

export default Dashboard;
